/*
 * servo_impl.hpp

 *
 *  Created on: 2016/07/08
 *      Author: tomoya
 */

#ifndef SERVO_IMPL_HPP_
#define SERVO_IMPL_HPP_

/*
#include <servo.hpp>
#include <algorithm>
#include <i2c/i2c.hpp>
#include <serial_connected_mcu/serial_connected_mcu_master.hpp>

template <class T>
servo<T>::servo(float kp, float ki, float kd) : _position(float()),
													 _pid(kp, ki, kd) {}

template <class T>
float servo<T>::operator()(float p) {
	return set(p);
}

template <class T>
float servo<T>::get(float p) {
	p = std::max(-1.0f, std::min(p, 1.0f));
	return _pid.update(p - current_pos);
}

template <class T>
float servo<T>::get() const {
	return _position;
}
*/
#endif
